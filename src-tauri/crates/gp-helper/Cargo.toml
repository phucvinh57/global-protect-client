[package]
name = "gp-helper"
version = "0.1.0"
edition = "2021"

[dependencies]
gp-auth = { path = "../gp-auth" }
openconnect = { path = "../openconnect" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
